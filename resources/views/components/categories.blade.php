<div class="relative">
    <a href="categories/{{$slug}}">
        <div class="lg:border p-1 md:p-2 absolute right-1 md:right-4 top-1 md:top-2 bg-gray-600 hover:bg-gray-800 rounded-sm">
            <h2 class="text-sm md:text-base capitalize font-bold text-white font-serif">
                {{$name}}
            </h2>
        </div>
        <img src="{{$image}}" alt="{{$name}}" class="w-full h-full" />
    </a>
</div>